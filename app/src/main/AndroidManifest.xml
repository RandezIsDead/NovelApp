<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Novel"
        tools:targetApi="31">
        <activity
            android:name=".Activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.VerifyEmailActivity" />
        <activity android:name=".Activities.Registration.EnterNameActivity" />
        <activity android:name=".Activities.Registration.EnterBirthDateActivity" />
        <activity android:name=".Activities.Registration.LocationActivity" />
        <activity android:name=".Activities.Registration.LocationManualActivity" />
        <activity android:name=".Activities.Registration.AddPhotosActivity" />
        <activity android:name=".Activities.Registration.GenderActivity" />
        <activity android:name=".Activities.Registration.AboutMeActivity"/>
        <activity android:name=".Activities.Registration.ExtraDataActivity"/>
        <activity android:name=".Activities.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>